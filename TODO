## Bugs
. Audio for explosion finish in panic, but not lasers. Only in Debug mode, this seems to be a known rodio issue.
. First firing is 'doubled', seems issue with timer.delta_seconds 
. `Result::unwrap()` on an `Err` value: NoSuchEntity', double despawning of lasers or asteroids ? 